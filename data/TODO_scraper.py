# =============================================================================
# TODO_scraper.py CURRENT CAPABILITIES
# =============================================================================

# ✅ COMPLETED(Jan 20):
# - fetch_page(): Basic HTTP request, returns HTML
# - parse_page(): Extracts title + main content from HTML
# - get_urls_from_sitemap(): Parses sitemap.xml for all doc URLs
# - scrape_all_docs(): Loops through URLs, saves results to JSON

# 🔧 ADD LATER (if needed to improve quality):
# - extract_metadata(): Add if you need dates, sections, canonical URLs
# - clean_content(): Add if chunks have too much whitespace/noise
# - code_block_extraction(): Add if code examples are getting mangled in chunks
# - e
